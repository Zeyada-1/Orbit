[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["cd questlist/backend && npm install"]

[phases.build]
cmds = ["cd questlist/backend && npx prisma generate"]

[start]
cmd = "cd questlist/backend && node src/index.js"
